name: release

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Update CHANGELOG
        id: changelog
        uses: requarks/changelog-action@v1
        with:
          token: ${{ github.token }}
          tag: ${{ github.ref_name }}
          writeToFile: false

      - name: Upload Github Release
        run: gh release create "${GITHUB_REF}" --notes "${{ steps.changelog.outputs.changes }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
